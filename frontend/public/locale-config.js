// Placeholder for local dev (npm run dev / vite build). In the Docker image
// this file is regenerated at container startup from DEFAULT_LOCALE - see
// frontend/docker-entrypoint.sh.
window.__APP_LOCALE__ = undefined;
